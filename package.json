{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "publish": "yarn workspaces foreach --exclude @lib-lib/random --exclude @lib-lib/signature -Apt run publish",
    "prepare": "yarn workspaces foreach --exclude @lib-lib/random --exclude @lib-lib/signature -Apt run prepare",
    "build": "yarn workspaces foreach --exclude @lib-lib/random --exclude @lib-lib/signature -Apt run build",
    "test": "yarn workspaces foreach --exclude @lib-lib/random --exclude @lib-lib/signature -Apt run test",
    "lint": "yarn workspaces foreach --exclude @lib-lib/random --exclude @lib-lib/signature -Apt run lint",
    "lint:fix": "yarn workspaces foreach --exclude @lib-lib/random --exclude @lib-lib/signature -Apt run lint:fix",
    "format": "yarn workspaces foreach --exclude @lib-lib/random --exclude @lib-lib/signature -Apt run format",
    "clean": "yarn workspaces foreach --exclude @lib-lib/random --exclude @lib-lib/signature -Apt run clean",
    "prepublish": "yarn build",
    "start": "tsx src/index.ts"
  },
  "packageManager": "yarn@4.0.2",
  "dependencies": {
    "tsx": "^4.1.4"
  }
}
