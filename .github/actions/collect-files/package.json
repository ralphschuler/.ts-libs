{
  "name": "collect-files-action",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "npx @vercel/ncc build && npx convert-action",
    "package": "npx @vercel/ncc build src/index.js -o dist"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "glob": "^7.2.3"
  }
}
